---
name: video-to-director-script
description: 把一个视频（本地 mp4/mov/mkv/webm 等）解构成导演级分镜脚本。通过程序化镜头切分与稠密光流分析，把视频转成一帧一帧可读的画面接触表，再由模型逐帧识别画面内容、人物动作、配音配乐、画面情绪与画面质感，最终按指定模板输出含 SEGMENT 通用约束栏与逐镜 14 字段（叙述/机位/镜头/构图/运镜/画面动态/光影/色号/材质/声音/对白/本镜NOT/Style Lock）的专业脚本。当用户提出"读取视频输出脚本""视频解构""视频转分镜""导出导演脚本""反推提示词""拆解这个视频怎么拍的"等需求时使用。
agent_created: true
---

# 视频 → 导演级分镜脚本

## 这个 Skill 做什么

把视频拆成可逐帧阅读的画面，测出**客观指标**（色号、色温、明暗比、暗部占比、颗粒、锐度、
景深、响度、BPM、卡点率、静默留白），再叠加**模型的视觉理解**（画面内容、人物动作、情绪、叙事），
最后按模板输出导演级分镜脚本。

**核心分工**（这条决定整个 Skill 的可靠性）：

- 程序负责确定性测量 —— 不猜数字。
- 模型负责语义解读 —— 读图后判断"发生了什么、什么情绪、像谁拍的"。
- 实测数字必须被引用进脚本；不得凭想象填 `[色号]` / `[光影]` / `[材质]`。

**约定**：本文件中的 `<SKILL>` 指本 Skill 所在目录（含 `SKILL.md` 的那一层），
`<PY>` 指带依赖的 Python 解释器。所有命令都用这两个占位符，不要写死绝对路径。

---

## 前置条件

**Python 3.10+**。依赖装在本项目（或任意虚拟环境）里即可：

```bash
<PY> -m pip install -r <SKILL>/requirements.txt
```

依赖清单：`numpy` `opencv-python-headless<5` `pillow` `imageio-ffmpeg`。
其中 `imageio-ffmpeg` 自带 ffmpeg 二进制，**不需要系统单独安装 ffmpeg**。

> **国内网络提示**：部分镜像源（如清华源）缺 `opencv-python-headless` 与 `imageio-ffmpeg`，
> 会报 `No matching distribution found`。此时换源即可，例如
> `-i https://mirrors.aliyun.com/pypi/simple/`。

**自检**（首次使用或换机器后建议跑一遍，确认全链路可用）：

```bash
<PY> <SKILL>/scripts/run_pipeline.py --selftest
```

该命令会现场合成一段测试视频并跑完整流程到临时目录，打印五步结果。
出现 `[pipeline] 完成` 且不再报错即为正常。

**输入**：本地视频文件。若用户给的是链接（B 站 / 抖音 / YouTube 等），
先下载成本地文件再进入流程；不要跳过下载直接开工。

### 模板准备（重要）

输出格式由模板决定，模板**不随本仓库分发**（版权归属可能不属于本项目）。
本 Skill 按以下优先级取模板：

1. `<SKILL>/references/output_template.md` —— 你自己的模板，放这里，优先使用
2. 用户在本轮对话中直接给出的模板内容
3. 都没有时，用 `references/script_template.md` 里的**字段清单**组装一个通用结构，
   并明确告诉用户「当前用的是通用结构，如需与你的模板逐字一致，请把模板放到
   `references/output_template.md`」

`references/script_template.md` 里的**字段含义、硬性要求、数据来源、降级写法、自检清单**
无论用哪份模板都适用，必须遵守。

---

## 工作流程

### 第 1 步 · 跑流水线

```bash
<PY> <SKILL>/scripts/run_pipeline.py "<视频路径>" --out "<输出目录>"
```

默认输出到 `<视频所在目录>/vds_out/<视频名>/`。建议显式 `--out` 到工作区，便于交付。

常用参数：

| 参数 | 用途 |
|---|---|
| `--budget N` | 覆盖全局关键帧预算（默认按时长自适应；镜头多时调小省 token） |
| `--tile 440` | 接触表单格宽度（px）。画面信息密（文字/细节多）时调到 560–640 |
| `--blocks 0.5` | 音频分块粒度（秒）。对白密集调到 0.25 |
| `--skip-audio` | 无音轨素材可跳过音频分析 |

流水线产物：

```
<out>/
  meta.json            素材元数据
  shots.json           镜头切分 + 关键帧索引 + 光流运镜指标
  shots_metrics.json   逐镜客观指标（含全片聚合）
  audio.json           响度/分段/节拍/卡点/静默
  context_pack.md      ★ 模型的主要事实来源
  frames/              关键帧 JPEG（文件名含镜号与毫秒时间码）
  sheets/overview_*.jpg  全局接触表（20 格/张，烧入时间码）
  sheets/detail_sXX.jpg  逐镜接触表（≤9 格，烧入时间码）
```

### 第 2 步 · 读 context_pack.md

**全文读完**。这是所有实测数字的汇总：素材概况、全片画像、镜头总表、逐镜明细、
接触表清单、读图指引、音频时间轴。不要用别的 JSON 代替它——`audio.json` 含逐块明细，
体积随片长线性增长，长视频不要整篇读。

### 第 3 步 · 三遍读图法

| 遍次 | 读什么 | 目的 |
|---|---|---|
| 1 粗读 | 全部 `sheets/overview_*.jpg`，按序 | 把握整体内容、场景流转、情绪走向、结构分拍 |
| 2 精读 | 每个镜头的 `sheets/detail_sXX.jpg` | 判定景别、机位、焦段、运镜、人物动作、表演细节 |
| 3 补读 | 关键镜头的单帧 `frames/*.jpg` | 确认瞬间细节（手部、道具、视线、文字） |

**硬性要求**：写任何一个镜头前，必须已读过它自己的 detail 接触表。
镜头数量多时分批读，但不得跳过。

### 第 4 步 · 写脚本

按第「模板准备」一节确定的模板输出。落笔时：

- 用 `references/analysis_rules.md` 把实测数字翻成术语（景别 / 光圈 / 明暗比 / 调色风格 /
  情绪 / 声音）。尤其是 **§7 情绪四步判定法** —— 不要跳过步骤直接给情绪词。
- 用 `references/style_lock_library.md` 选风格参照与核心张力原型，必须与实测指纹对得上。
- 每个镜头的 `[色号]` 直接取该镜实测 palette 的四个 hex。
- 无 ASR：`[对白]` 只能写「有对白 + 起止时间码 + 波形推测的语气语速」，**不得编造词句**。
  若素材自带内嵌字幕轨，可用 `vds_common.run_ffmpeg` 抽出字幕补齐原文：

  ```bash
  <PY> -c "import sys;sys.path.insert(0,r'<SKILL>/scripts');from vds_common import run_ffmpeg;print(run_ffmpeg(['-y','-i',r'<视频>','-map','0:s:0',r'<输出.srt>'])[1][-800:])"
  ```

### 第 5 步 · 自检

逐项核对 `references/script_template.md` §六 的质量自检清单。重点三项：

1. 每个镜头的 `[色号]` / `[光影]` / `[材质]` 都有实测依据；
2. 时间码连续、无重叠、总和等于素材时长；
3. 没有凭空编造的对白。

### 第 6 步 · 交付

最终脚本写到 `<out>/<视频名>_导演脚本.md`，用 `present_files` 一并展示
脚本与关键接触表（`sheets/overview_01.jpg` 等）。

---

## 输出物命名

| 文件 | 内容 |
|---|---|
| `<视频名>_导演脚本.md` | 主交付物，按模板的完整分镜脚本 |
| `context_pack.md` | 实测数据汇总（作为脚本的证据附录） |
| `sheets/*.jpg` | 画面接触表，便于人工核对脚本与画面是否对应 |

---

## 使用实测数据时的纪律

这几条是实测踩出来的，写脚本时必须遵守：

1. **`light_ratio_stops` 是「画面明暗比」，不是布光光比。** 它测的是整幅画面的亮部/暗部
   亮度比（分母已设 5/255 下限、上限封顶 6 级，避免死黑导致数值爆炸）。布光光比是主体上的
   照度比，从成片测不出来。措辞用「画面明暗比」，或定性描述「主光强、辅光弱」。
2. **`fg_bg_sharpness_ratio` 只是景深的弱线索。** 背景是平滑面（天空/纯色墙/渐变底）时该值
   会虚高（实测见过 33.2，实际是全清的图形画面）。**必须读图确认景深**再写光圈。
3. **运镜看光流四件套，不要看 `motion`。** `motion` 是直方图+缩略图差异，只用于辅助判断
   切点强度；`cam_move` / `cam_zoom` / `cam_dx` / `cam_dy` 才是运镜依据，
   `local_motion`（P90 残差）才是主体动作依据。
4. **`skin_ratio` 会误报。** 暖米色、食物、木质、黄昏天空都会触发。它只是「可能有人的线索」，
   必须读图确认。
5. **音频分类是启发式，一律标「疑似」。** 无 ASR 时不得凭分类结果编造台词。
6. **判定为 AI 生成素材时必须显式注明**，并把 AI 伪影（手部畸形/文字乱码/帧间闪烁等）
   写进 `[本镜NOT]`。

---

## 常见坑

- **中文路径**：`cv2.imread` / `cv2.imwrite` 在非 ASCII 路径下会**静默失败**
  （返回 False 但不抛异常），结果是 `frames/` 全空、接触表全黑。流水线已改用
  `imwrite_unicode` / `imread_unicode`（`cv2.imencode` + 文件句柄）。
  **新增任何图像读写必须走这两个函数，不要直接用 cv2 的。**
- **竖屏视频**：OpenCV 可能不自动应用旋转元数据。若发现接触表里画面是躺倒的，
  在 `extract_keyframes.py` 的 `main()` 里给帧加 `cv2.rotate(frame, cv2.ROTATE_90_CLOCKWISE)`。
- **镜头数暴涨**：闪光、频闪、快速推拉会被误判为切换。此时提高 `extract_keyframes.py` 里
  `detect_shots()` 的 `min_shot`（默认按总时长自动取 0.2 / 0.35 / 0.6s）。
- **接触表太大读不清**：提高 `--tile`（让单格更大），而不是增加格数。信息密的画面用 560–640。
- **预算失控**：长视频（>3min）先跑 `--budget 120` 出粗版，确认结构后再对重点段落单独重跑。
- **无音轨**：`audio.json` 的 `has_audio` 为 false，按 `script_template.md` §四 的降级写法处理。
- **接触表时间码**：格式为 `S<镜号> <秒数>`，读图时用它把画面与时间轴对齐。

---

## 相关文件

| 文件 | 用途 |
|---|---|
| `references/script_template.md` | 字段含义、硬性要求、数据来源、降级写法、自检清单 |
| `references/analysis_rules.md` | 实测数字 → 导演术语的映射规则（含情绪四步判定法） |
| `references/style_lock_library.md` | 导演/影片风格指纹卡、核心张力原型、情绪弧线结构 |
| `references/output_template.md` | **用户自备**的输出模板骨架（不进版本库） |
| `scripts/build_skill.py` | 把本仓库打包/安装成标准 Skill 目录 |
