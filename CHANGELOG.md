# 更新记录

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] - 2026-09-23

首个公开版本。

### 新增

- **镜头切分**：HSV 直方图 + 灰度缩略图差异，用中位数/MAD 自适应阈值，按总时长自动取最小镜长。
- **稠密光流运镜分析**：把运动分解为全局位移（`cam_move` / `cam_dx` / `cam_dy`）、
  散度（`cam_zoom`，推近/拉远）、P90 残差（`local_motion`，主体动作）。
  可区分「摄影机在动」与「主体在动」。
- **自适应关键帧抽取**：按总时长决定全局预算，按镜头时长加权分配（每镜 3–14 帧），
  镜内去重但保留下限与镜头末帧。
- **双档接触表**：全局 `overview`（20 格/张）与逐镜 `detail`（≤9 格/张），
  时间码与镜号烧入画面，便于与时间轴对齐。
- **逐镜影像指标**：调色板四角色 hex（k-means）、亮度五数、暗部/高光占比、对比度、
  画面明暗比、估计色温、饱和度、颗粒 σ、锐度、中心/边缘清晰度比、肤色占比、
  分离调色判定、黑边检测。
- **音频波形分析**（无需语音识别）：积分 LUFS / LRA / 真峰值、六段频段画像、
  静音留白 / 疑似人声 / 疑似音乐 / 环境氛围分段（带置信度）、自相关 BPM、
  **卡点率**（切换点与节拍网格对齐度）、静默区间。
- **`context_pack.md`**：把所有实测数据汇总为单份模型可读的事实来源。
- **知识文档**：`references/script_template.md`（字段规范与自检清单）、
  `references/analysis_rules.md`（实测数字 → 导演术语映射，含情绪四步判定法）、
  `references/style_lock_library.md`（导演风格指纹卡、核心张力原型、情绪弧线结构）。
- **`build_skill.py`**：把扁平仓库打包/安装为标准 Skill 目录，跨平台、无需符号链接。
- **`run_pipeline.py --selftest`**：现场合成测试视频跑通全链路，用于验证环境。

### 修复（开发期踩坑记录）

- `cv2.imread` / `cv2.imwrite` 在非 ASCII 路径下**静默失败**，导致关键帧全空、接触表全黑。
  改用 `cv2.imencode` + 文件句柄的 Unicode 安全读写。
- ffmpeg `ebur128` 的逐帧进度行会被正则抢先匹配，得到 −70 LUFS 这类假值。
  改为只解析 `Summary:` 段。
- 画面明暗比在遇到死黑时数值爆炸（曾算出 3327:1）。改为亮部/暗部各取 15% 分位、
  分母设 5/255 下限、级数封顶 6 级，并明确其语义不是布光光比。
- 景深线索原用不连续边界像素做差分方差，得到无意义值（0.0176）。
  改为对四条边缘带分别计算拉普拉斯方差再平均，并降级标注为弱线索。
- 光流推拉门限原设 0.02/秒，会把缓慢电影推镜（实测 0.01–0.02/秒）误判为「无推拉」。
  重新标定为 0.01 / 0.04 / 0.12 三档。
- 镜内去重过于激进，静态镜头会被压到 1–2 帧、丢失动作信息。
  改为「直方图与缩略图都几乎不变才丢帧」，并保证每镜 ≥3 帧、镜头末帧必留。
